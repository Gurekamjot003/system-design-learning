#pragma once
#include<bits/stdc++.h>
#include"SplitType.hpp"

class Split{
public:
    string user_id;
    double amount;

    Split(string user_id, double amount):user_id(user_id), amount(amount){}

};